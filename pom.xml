<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>fi.vm.sade.organisaatio</groupId>
    <artifactId>organisaatio</artifactId>
    <version>8.0</version>
    <packaging>pom</packaging>
    <name>Organisaatio :: Parent</name>


    <parent>
        <groupId>fi.vm.sade.generic</groupId>
        <artifactId>build-parent</artifactId>
        <version>8.0</version>
    </parent>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.build.resourceEncoding>UTF-8</project.build.resourceEncoding>
        <authentication.api.version>7.0</authentication.api.version>
        <generic.version>8.0</generic.version>
        <sade-generic.version>8.0</sade-generic.version>
        <koodisto.api.version>7.0</koodisto.api.version>
        <koodisto.widget.version>7.0</koodisto.widget.version>
        <log.client.version>8.0</log.client.version>
        <oid.api.version>8.0</oid.api.version>
        <rajapinnat.ytj.version>8.0</rajapinnat.ytj.version>
        <vaadin.theme.version>8.0</vaadin.theme.version>

        <ehcache.version>2.5.0</ehcache.version>
        <ehcache-jgroupsreplication.version>1.5</ehcache-jgroupsreplication.version>
        <jgroups.version>2.10.0.GA</jgroups.version>
    </properties>

    <modules>
        <module>organisaatio-api</module>
        <module>organisaatio-shared</module>
        <module>organisaatio-solr-client</module>
        <module>organisaatio-service</module>
        <module>organisaatio-ui-widgets</module>
        <module>organisaatio-app</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>net.sf.ehcache</groupId>
                <artifactId>ehcache-core</artifactId>
                <version>${ehcache.version}</version>
            </dependency>

            <dependency>
                <groupId>net.sf.ehcache</groupId>
                <artifactId>ehcache-jgroupsreplication</artifactId>
                <version>${ehcache-jgroupsreplication.version}</version>
            </dependency>

            <dependency>
                <groupId>org.jgroups</groupId>
                <artifactId>jgroups</artifactId>
                <version>${jgroups.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <repositories>
        <repository>
            <id>oph-sade-artifactory-snapshots</id>
            <url>http://penaali.hard.ware.fi/artifactory/oph-sade-snapshot-local</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>oph-sade-artifactory-releases</id>
            <url>http://penaali.hard.ware.fi/artifactory/oph-sade-release-local</url>
        </repository>
    </repositories>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>1.2</version>
                <executions>
                    <execution>
                        <id>enforce-java</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[1.6,1.7)</version>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
