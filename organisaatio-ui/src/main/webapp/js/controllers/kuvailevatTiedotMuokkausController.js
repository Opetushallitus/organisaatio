function KuvailevatTiedotMuokkausController($scope, $modalInstance, $log, nimi, field, lang, data, options) {
    $scope.nimi = nimi;
    $scope.field = field;
    $scope.lang = lang;
    $scope.data = data;
    $scope.options = options;
}

