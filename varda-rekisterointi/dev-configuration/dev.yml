server:
  ssl:
    enabled: true
    keyStore: classpath:dev/keystore.jks
    keyStorePassword: changeit
    keyPassword: changeit
varda-rekisterointi:
  url-virkailija: https://localhost:8080
  url-tunnistus: https://localhost:8080
