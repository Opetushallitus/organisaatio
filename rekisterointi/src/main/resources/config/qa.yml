server.servlet.session.cookie.domain: testiopintopolku.fi
spring.data.redis.host: ${rekisterointi_redis_host}

url-oppija:  https://testiopintopolku.fi
url-virkailija: https://virkailija.testiopintopolku.fi
url-rekisterointi: https://rekisterointi.testiopintopolku.fi
url-alb: testiopintopolku.fi

rekisterointi:
    service:
        username: ${varda_rekisterointi_service_username}
        password: ${varda_rekisterointi_service_password}
    valtuudet:
        host: ${varda_rekisterointi_valtuudet_host}
        client-id: ${varda_rekisterointi_valtuudet_client_id}
        api-key: ${varda_rekisterointi_valtuudet_api_key}
        oauth-password: ${varda_rekisterointi_valtuudet_oauth_password}
varda-rekisterointi:
    url: https://virkailija.testiopintopolku.fi/varda-rekisterointi/api/rekisterointi
    username: ${varda_rekisterointi_rekisterointi_ui_username}
    password: ${varda_rekisterointi_rekisterointi_ui_password}
